#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
APP_PATH="$SCRIPT_DIR/batView.app"

if [[ ! -d "$APP_PATH" ]]; then
    echo "batView.app not found next to this launcher."
    exit 1
fi

open "$APP_PATH"